// Code generated by entc, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"

	"github.com/blushft/strana/platform/store/reporter/ent/pagestat"
	"github.com/blushft/strana/platform/store/reporter/ent/pathname"
	"github.com/blushft/strana/platform/store/reporter/ent/predicate"
	"github.com/facebook/ent/dialect/sql"
	"github.com/facebook/ent/dialect/sql/sqlgraph"
	"github.com/facebook/ent/schema/field"
)

// PathnameUpdate is the builder for updating Pathname entities.
type PathnameUpdate struct {
	config
	hooks      []Hook
	mutation   *PathnameMutation
	predicates []predicate.Pathname
}

// Where adds a new predicate for the builder.
func (pu *PathnameUpdate) Where(ps ...predicate.Pathname) *PathnameUpdate {
	pu.predicates = append(pu.predicates, ps...)
	return pu
}

// SetName sets the name field.
func (pu *PathnameUpdate) SetName(s string) *PathnameUpdate {
	pu.mutation.SetName(s)
	return pu
}

// AddPageStatIDs adds the page_stats edge to PageStat by ids.
func (pu *PathnameUpdate) AddPageStatIDs(ids ...int) *PathnameUpdate {
	pu.mutation.AddPageStatIDs(ids...)
	return pu
}

// AddPageStats adds the page_stats edges to PageStat.
func (pu *PathnameUpdate) AddPageStats(p ...*PageStat) *PathnameUpdate {
	ids := make([]int, len(p))
	for i := range p {
		ids[i] = p[i].ID
	}
	return pu.AddPageStatIDs(ids...)
}

// Mutation returns the PathnameMutation object of the builder.
func (pu *PathnameUpdate) Mutation() *PathnameMutation {
	return pu.mutation
}

// RemovePageStatIDs removes the page_stats edge to PageStat by ids.
func (pu *PathnameUpdate) RemovePageStatIDs(ids ...int) *PathnameUpdate {
	pu.mutation.RemovePageStatIDs(ids...)
	return pu
}

// RemovePageStats removes page_stats edges to PageStat.
func (pu *PathnameUpdate) RemovePageStats(p ...*PageStat) *PathnameUpdate {
	ids := make([]int, len(p))
	for i := range p {
		ids[i] = p[i].ID
	}
	return pu.RemovePageStatIDs(ids...)
}

// Save executes the query and returns the number of rows/vertices matched by this operation.
func (pu *PathnameUpdate) Save(ctx context.Context) (int, error) {

	var (
		err      error
		affected int
	)
	if len(pu.hooks) == 0 {
		affected, err = pu.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*PathnameMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			pu.mutation = mutation
			affected, err = pu.sqlSave(ctx)
			mutation.done = true
			return affected, err
		})
		for i := len(pu.hooks) - 1; i >= 0; i-- {
			mut = pu.hooks[i](mut)
		}
		if _, err := mut.Mutate(ctx, pu.mutation); err != nil {
			return 0, err
		}
	}
	return affected, err
}

// SaveX is like Save, but panics if an error occurs.
func (pu *PathnameUpdate) SaveX(ctx context.Context) int {
	affected, err := pu.Save(ctx)
	if err != nil {
		panic(err)
	}
	return affected
}

// Exec executes the query.
func (pu *PathnameUpdate) Exec(ctx context.Context) error {
	_, err := pu.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (pu *PathnameUpdate) ExecX(ctx context.Context) {
	if err := pu.Exec(ctx); err != nil {
		panic(err)
	}
}

func (pu *PathnameUpdate) sqlSave(ctx context.Context) (n int, err error) {
	_spec := &sqlgraph.UpdateSpec{
		Node: &sqlgraph.NodeSpec{
			Table:   pathname.Table,
			Columns: pathname.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: pathname.FieldID,
			},
		},
	}
	if ps := pu.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if value, ok := pu.mutation.Name(); ok {
		_spec.Fields.Set = append(_spec.Fields.Set, &sqlgraph.FieldSpec{
			Type:   field.TypeString,
			Value:  value,
			Column: pathname.FieldName,
		})
	}
	if nodes := pu.mutation.RemovedPageStatsIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.O2M,
			Inverse: false,
			Table:   pathname.PageStatsTable,
			Columns: []string{pathname.PageStatsColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: &sqlgraph.FieldSpec{
					Type:   field.TypeInt,
					Column: pagestat.FieldID,
				},
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_spec.Edges.Clear = append(_spec.Edges.Clear, edge)
	}
	if nodes := pu.mutation.PageStatsIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.O2M,
			Inverse: false,
			Table:   pathname.PageStatsTable,
			Columns: []string{pathname.PageStatsColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: &sqlgraph.FieldSpec{
					Type:   field.TypeInt,
					Column: pagestat.FieldID,
				},
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_spec.Edges.Add = append(_spec.Edges.Add, edge)
	}
	if n, err = sqlgraph.UpdateNodes(ctx, pu.driver, _spec); err != nil {
		if _, ok := err.(*sqlgraph.NotFoundError); ok {
			err = &NotFoundError{pathname.Label}
		} else if cerr, ok := isSQLConstraintError(err); ok {
			err = cerr
		}
		return 0, err
	}
	return n, nil
}

// PathnameUpdateOne is the builder for updating a single Pathname entity.
type PathnameUpdateOne struct {
	config
	hooks    []Hook
	mutation *PathnameMutation
}

// SetName sets the name field.
func (puo *PathnameUpdateOne) SetName(s string) *PathnameUpdateOne {
	puo.mutation.SetName(s)
	return puo
}

// AddPageStatIDs adds the page_stats edge to PageStat by ids.
func (puo *PathnameUpdateOne) AddPageStatIDs(ids ...int) *PathnameUpdateOne {
	puo.mutation.AddPageStatIDs(ids...)
	return puo
}

// AddPageStats adds the page_stats edges to PageStat.
func (puo *PathnameUpdateOne) AddPageStats(p ...*PageStat) *PathnameUpdateOne {
	ids := make([]int, len(p))
	for i := range p {
		ids[i] = p[i].ID
	}
	return puo.AddPageStatIDs(ids...)
}

// Mutation returns the PathnameMutation object of the builder.
func (puo *PathnameUpdateOne) Mutation() *PathnameMutation {
	return puo.mutation
}

// RemovePageStatIDs removes the page_stats edge to PageStat by ids.
func (puo *PathnameUpdateOne) RemovePageStatIDs(ids ...int) *PathnameUpdateOne {
	puo.mutation.RemovePageStatIDs(ids...)
	return puo
}

// RemovePageStats removes page_stats edges to PageStat.
func (puo *PathnameUpdateOne) RemovePageStats(p ...*PageStat) *PathnameUpdateOne {
	ids := make([]int, len(p))
	for i := range p {
		ids[i] = p[i].ID
	}
	return puo.RemovePageStatIDs(ids...)
}

// Save executes the query and returns the updated entity.
func (puo *PathnameUpdateOne) Save(ctx context.Context) (*Pathname, error) {

	var (
		err  error
		node *Pathname
	)
	if len(puo.hooks) == 0 {
		node, err = puo.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*PathnameMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			puo.mutation = mutation
			node, err = puo.sqlSave(ctx)
			mutation.done = true
			return node, err
		})
		for i := len(puo.hooks) - 1; i >= 0; i-- {
			mut = puo.hooks[i](mut)
		}
		if _, err := mut.Mutate(ctx, puo.mutation); err != nil {
			return nil, err
		}
	}
	return node, err
}

// SaveX is like Save, but panics if an error occurs.
func (puo *PathnameUpdateOne) SaveX(ctx context.Context) *Pathname {
	pa, err := puo.Save(ctx)
	if err != nil {
		panic(err)
	}
	return pa
}

// Exec executes the query on the entity.
func (puo *PathnameUpdateOne) Exec(ctx context.Context) error {
	_, err := puo.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (puo *PathnameUpdateOne) ExecX(ctx context.Context) {
	if err := puo.Exec(ctx); err != nil {
		panic(err)
	}
}

func (puo *PathnameUpdateOne) sqlSave(ctx context.Context) (pa *Pathname, err error) {
	_spec := &sqlgraph.UpdateSpec{
		Node: &sqlgraph.NodeSpec{
			Table:   pathname.Table,
			Columns: pathname.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: pathname.FieldID,
			},
		},
	}
	id, ok := puo.mutation.ID()
	if !ok {
		return nil, &ValidationError{Name: "ID", err: fmt.Errorf("missing Pathname.ID for update")}
	}
	_spec.Node.ID.Value = id
	if value, ok := puo.mutation.Name(); ok {
		_spec.Fields.Set = append(_spec.Fields.Set, &sqlgraph.FieldSpec{
			Type:   field.TypeString,
			Value:  value,
			Column: pathname.FieldName,
		})
	}
	if nodes := puo.mutation.RemovedPageStatsIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.O2M,
			Inverse: false,
			Table:   pathname.PageStatsTable,
			Columns: []string{pathname.PageStatsColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: &sqlgraph.FieldSpec{
					Type:   field.TypeInt,
					Column: pagestat.FieldID,
				},
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_spec.Edges.Clear = append(_spec.Edges.Clear, edge)
	}
	if nodes := puo.mutation.PageStatsIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.O2M,
			Inverse: false,
			Table:   pathname.PageStatsTable,
			Columns: []string{pathname.PageStatsColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: &sqlgraph.FieldSpec{
					Type:   field.TypeInt,
					Column: pagestat.FieldID,
				},
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_spec.Edges.Add = append(_spec.Edges.Add, edge)
	}
	pa = &Pathname{config: puo.config}
	_spec.Assign = pa.assignValues
	_spec.ScanValues = pa.scanValues()
	if err = sqlgraph.UpdateNode(ctx, puo.driver, _spec); err != nil {
		if _, ok := err.(*sqlgraph.NotFoundError); ok {
			err = &NotFoundError{pathname.Label}
		} else if cerr, ok := isSQLConstraintError(err); ok {
			err = cerr
		}
		return nil, err
	}
	return pa, nil
}
