// Code generated by entc, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"github.com/blushft/strana/modules/sink/reporter/store/ent/referrer"
	"github.com/facebook/ent/dialect/sql"
)

// Referrer is the model entity for the Referrer schema.
type Referrer struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Name holds the value of the "name" field.
	Name string `json:"name,omitempty"`
	// Type holds the value of the "type" field.
	Type string `json:"type,omitempty"`
	// Hostname holds the value of the "hostname" field.
	Hostname string `json:"hostname,omitempty"`
	// Link holds the value of the "link" field.
	Link string `json:"link,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the ReferrerQuery when eager-loading is set.
	Edges ReferrerEdges `json:"edges"`
}

// ReferrerEdges holds the relations/edges for other nodes in the graph.
type ReferrerEdges struct {
	// Events holds the value of the events edge.
	Events []*Event
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// EventsOrErr returns the Events value or an error if the edge
// was not loaded in eager-loading.
func (e ReferrerEdges) EventsOrErr() ([]*Event, error) {
	if e.loadedTypes[0] {
		return e.Events, nil
	}
	return nil, &NotLoadedError{edge: "events"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Referrer) scanValues() []interface{} {
	return []interface{}{
		&sql.NullInt64{},  // id
		&sql.NullString{}, // name
		&sql.NullString{}, // type
		&sql.NullString{}, // hostname
		&sql.NullString{}, // link
	}
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Referrer fields.
func (r *Referrer) assignValues(values ...interface{}) error {
	if m, n := len(values), len(referrer.Columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	value, ok := values[0].(*sql.NullInt64)
	if !ok {
		return fmt.Errorf("unexpected type %T for field id", value)
	}
	r.ID = int(value.Int64)
	values = values[1:]
	if value, ok := values[0].(*sql.NullString); !ok {
		return fmt.Errorf("unexpected type %T for field name", values[0])
	} else if value.Valid {
		r.Name = value.String
	}
	if value, ok := values[1].(*sql.NullString); !ok {
		return fmt.Errorf("unexpected type %T for field type", values[1])
	} else if value.Valid {
		r.Type = value.String
	}
	if value, ok := values[2].(*sql.NullString); !ok {
		return fmt.Errorf("unexpected type %T for field hostname", values[2])
	} else if value.Valid {
		r.Hostname = value.String
	}
	if value, ok := values[3].(*sql.NullString); !ok {
		return fmt.Errorf("unexpected type %T for field link", values[3])
	} else if value.Valid {
		r.Link = value.String
	}
	return nil
}

// QueryEvents queries the events edge of the Referrer.
func (r *Referrer) QueryEvents() *EventQuery {
	return (&ReferrerClient{config: r.config}).QueryEvents(r)
}

// Update returns a builder for updating this Referrer.
// Note that, you need to call Referrer.Unwrap() before calling this method, if this Referrer
// was returned from a transaction, and the transaction was committed or rolled back.
func (r *Referrer) Update() *ReferrerUpdateOne {
	return (&ReferrerClient{config: r.config}).UpdateOne(r)
}

// Unwrap unwraps the entity that was returned from a transaction after it was closed,
// so that all next queries will be executed through the driver which created the transaction.
func (r *Referrer) Unwrap() *Referrer {
	tx, ok := r.config.driver.(*txDriver)
	if !ok {
		panic("ent: Referrer is not a transactional entity")
	}
	r.config.driver = tx.drv
	return r
}

// String implements the fmt.Stringer.
func (r *Referrer) String() string {
	var builder strings.Builder
	builder.WriteString("Referrer(")
	builder.WriteString(fmt.Sprintf("id=%v", r.ID))
	builder.WriteString(", name=")
	builder.WriteString(r.Name)
	builder.WriteString(", type=")
	builder.WriteString(r.Type)
	builder.WriteString(", hostname=")
	builder.WriteString(r.Hostname)
	builder.WriteString(", link=")
	builder.WriteString(r.Link)
	builder.WriteByte(')')
	return builder.String()
}

// Referrers is a parsable slice of Referrer.
type Referrers []*Referrer

func (r Referrers) config(cfg config) {
	for _i := range r {
		r[_i].config = cfg
	}
}
